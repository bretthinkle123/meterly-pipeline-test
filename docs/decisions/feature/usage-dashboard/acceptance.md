---
criteria_total: 25
feature: "Meterly — read-only Usage Dashboard (feature 3, front-end)"
derived_from: >
  PROJECT.md "This build" (faithful SCREEN-1 dashboard, backed entirely by GET /v1/usage) +
  the human-vouched .pipeline/design-spec.md (SCREEN-1, CMP-1..CMP-9, tokens) +
  pipeline input-surface / DAST / data-protection / iac conventions.
notes: >
  Front-end feature served BY the existing FastAPI app. No new backend DATA endpoint beyond the
  serving/BFF layer, no schema change, no new data store, NO migration, NO new RUNTIME dependency
  (two DEV/test deps proposed: pytest-playwright==0.8.0 + playwright==1.61.0, exact pins, flagged Q4).
  One small additive infra delta: a Secrets Manager secret for the reader credential + a
  least-privilege ECS task-role grant (AC25). The design-spec is authoritative and design-approved
  (current hash), so each SCREEN-1/CMP-n is traced to a criterion (design -> plan -> acceptance).
  Input sources exposed: exactly ONE untrusted source — the GET /dashboard/api/usage-series query
  string (customer_id, metric, granularity); per the input-surface rule it carries a validation
  criterion (AC11, not waivable) AND a rate-limit criterion (AC12). The feature stores no new user
  data, so the data-protection obligation is a WAIVER (AC19). Perf budget AC16 (25 req/s) rides
  criteria_covered + perf-pairing. The load-bearing security property is AC9 (NO client-side API key).
  DAST Layer 1 is enabled (.pipeline/dast.env) -> AC17/AC18. Feature-1/2 backend criteria are
  unchanged and NOT re-listed. Revised once after plan-audit: AC16 concurrency quantified, AC24
  (safe-error) + AC25 (IaC) added, AC21 dep pins corrected, AC6 skeleton row count fixed.
---

# Acceptance criteria — Meterly feature 3 (Usage Dashboard)

| ID | Criterion | File / layer | How verified | Traces |
|---|---|---|---|---|
| AC1 | **`GET /dashboard` serves the SCREEN-1 page:** returns **200** with `Content-Type: text/html`, the served-page **CSP** (`default-src 'none'; script-src 'self'; style-src 'self'; img-src 'self' data:; connect-src 'self'; base-uri 'none'; frame-ancestors 'none'; object-src 'none'; form-action 'none'`) and the full inherited header set (HSTS, `nosniff`, `Referrer-Policy`, `Permissions-Policy`). The two static assets (`dashboard.css`, `dashboard.js`) also serve **200**. | `src/api/routes/dashboard.py`, `src/api/middleware.py`, `src/web/static/*` | integration: GET the page + assets, assert 200 + exact header/CSP set | SCREEN-1 |
| AC2 | **Header replicated (`CMP-1`+`CMP-2`):** the page contains the top bar — logo mark "M" (`color/accent` `#2563eb`), wordmark "Meterly", breadcrumb "/ Usage", and the environment badge `CMP-2`. Structure present with stable `data-testid`s. | `src/web/static/dashboard.html`, `dashboard.css` | integration (HTML structure) + E2E visual presence | `CMP-1`, `CMP-2` |
| AC3 | **Filter row replicated (`CMP-3`+`CMP-4`):** Customer + Metric native `<select>`s (`CMP-3`, real `:hover`/`:focus` ring) and the Window segmented control (`CMP-4`, options **hour/day/month**, active-segment styling, keyboard-operable focus outline) render per tokens. | `dashboard.html`, `dashboard.css`, `dashboard.js` | integration (structure) + E2E (segmented control keyboard-operable, focus ring) | `CMP-3`, `CMP-4` |
| AC4 | **Populated state replicated (`CMP-5`/`CMP-6`/`CMP-7`/`CMP-8`):** stat card with the big number (`type/display` 58/650, `tabular-nums`), a `CMP-6` delta pill, a `CMP-8` metric chip + window noun; and the "Recent windows" table (`CMP-7`, caption "last 10 · newest first", grid columns Window start / Metric / Quantity (right) / Δ vs prior (right), 10 rows, `:hover` row bg `#f6f8fb`) with a `CMP-6` delta pill per row. | `dashboard.html`, `dashboard.css`, `dashboard.js` | E2E: seed data, assert stat card + 10 rows + per-row deltas render per structure | `CMP-5`,`CMP-6`,`CMP-7`,`CMP-8` |
| AC5 | **Empty state replicated (`CMP-9`), real trigger:** when the selection has **no usage rows** (all 11 windows zero — NOT the mock's hardcoded `initech`), the centered empty card renders ("No events yet for {customer}", `POST /v1/events` hint, `{ }` glyph). | `dashboard.js`, `dashboard.html`, `src/services/dashboard_service.py` | integration (BFF `state:"empty"` for a zero customer) + E2E (empty card renders) | `CMP-9` |
| AC6 | **Loading state (new, designed):** on initial load **and on every filter change**, a skeleton mirroring the stat-card + table layout renders **exactly 10 placeholder rows** (matching the populated table) using surface/border/muted tokens, until data arrives; the layout does not shift when data lands. | `dashboard.js`, `dashboard.css` | E2E: throttle/stub the BFF, assert the 10-row skeleton appears on load and on a filter change | design-spec §1/§6 open item |
| AC7 | **Error state (new, designed):** when the BFF returns non-200 or the fetch fails, an error card (reusing `CMP-9`'s structure) renders with a **generic** message (no raw error/stack/SQL) and a working **Retry** control that re-fetches. | `dashboard.js`, `dashboard.css` | E2E: force a BFF failure, assert the error card + generic copy + retry re-fetch | design-spec §1/§6 open item |
| AC8 | **Data sourced from `GET /v1/usage`, NOT the PRNG mock:** the big number = the newest window's total; the 10 rows = the last 10 windows; each delta = window *i* vs window *i+1* (11 values → 10 deltas). Values come from `get_usage` reads assembled server-side by the BFF; no synthetic `renderVals`/`rng`/`series` code exists in the shipped page. | `src/services/dashboard_service.py`, `src/api/routes/dashboard.py`, `dashboard.js` | integration: seed `usage_rollup`, assert BFF series = seeded values + correct deltas; grep the shipped JS for no PRNG mock | design-spec §5/§6 (substantive item 1) |
| AC9 | **NO client-side API key (load-bearing):** the served HTML/CSS/JS **and** every `/dashboard/api/*` response contain **no `mtr_live` token and no `Authorization` value**; the browser sends **no credential** on any dashboard request; the reader key is server-held from Secrets Manager (`get_secret`), never serialized to the browser. | `src/auth/dashboard_reader.py`, `src/config/secrets.py`, `dashboard.js` | integration: assert served assets + BFF JSON contain no `mtr_live`/`Authorization`; E2E: capture network, assert no credential leaves the browser; Gitleaks (no hardcoded secret) | plan §Auth (no browser key) |
| AC10 | **Tenant isolation / IDOR through the data path:** the BFF reads **only** the reader principal's tenant via `get_usage`'s `api_key_id` scoping + PostgreSQL RLS. Rows written under a **different** `api_key_id` never appear in the BFF output; a foreign/unknown `customer_id` yields the empty state (zeros, no existence oracle), never another tenant's data. | `src/services/dashboard_service.py`, `src/repositories/usage_repo.py`, `src/db/session.py` | integration cross-tenant test: seed rows under key A + reader key B, assert the BFF (as B) never returns A's rows | ASVS 8.2.2 (IDOR/BOLA) |
| AC11 | **BFF input validation (input-surface, NOT waivable):** `GET /dashboard/api/usage-series` enforces `customer_id` (`^[A-Za-z0-9_.:-]{1,128}$` **and** ∈ configured `customers` allowlist), `metric` (`^[A-Za-z0-9_.:-]{1,64}$` **and** ∈ `metrics` allowlist), `granularity` ∈ `{hour, day}`, `extra='forbid'`. Malformed / out-of-allowlist / wrong-type / unknown-param → **422**. Injection payloads (`' OR 1=1--`, `<script>`, `../`, CRLF) in `customer_id`/`metric` are rejected at the boundary allowlist and parameterized at the `get_usage` SQL sink. | `src/api/schemas/dashboard.py`, `src/repositories/usage_repo.py` | Hypothesis property tests + explicit malformed/injection tests asserting 422 + no SQL reflection | input-surface validation (not waivable) |
| AC12 | **BFF rate limit (input-surface):** `GET /dashboard/api/usage-series` — an **anonymous edge** endpoint (no client principal) — is throttled by the inherited **Tier-1 IP+route** token bucket; over-limit → **429 + `Retry-After`**. (Per-owner Tier-2 does not apply: there is no client principal at the viewer boundary; the two-principals-one-IP shape is N/A here.) | `src/auth/rate_limit.py` (Tier-1, inherited), `src/api/middleware.py` | integration: exhaust the Tier-1 bucket for the BFF route from one IP → 429 + `Retry-After` | input-surface rate-limit (Tier-1 anonymous edge) |
| AC13 | **XSS-safe rendering + CSP:** all dynamic usage values are written to the DOM via `textContent`/`createElement` — **never `innerHTML`**, no inline handlers, no `eval`; a seeded usage value containing `<script>`/markup renders as inert text and does **not** execute (proved end-to-end); the page CSP `script-src 'self'` (no `unsafe-inline`/`unsafe-eval`) is the backstop. | `src/web/static/dashboard.js`, `src/api/middleware.py` | E2E: seed an injection payload into a rendered value, assert it appears as text and executes no script; static check: shipped JS contains no `innerHTML` | ASVS 3.2.2, 3.4.3 |
| AC14 | **`no-store` on page + data:** `GET /dashboard` and every `/dashboard/api/*` response carry `Cache-Control: no-store` — usage totals / `customer_id` are not cached by the browser or an intermediary. | `src/api/middleware.py` | integration: assert `Cache-Control: no-store` on the page + BFF responses | ASVS 14.3.2 |
| AC15 | **Clickjacking defense:** the served page carries CSP `frame-ancestors 'none'` **and** `X-Frame-Options: DENY`. | `src/api/middleware.py` | integration: assert both headers on `GET /dashboard` | ASVS 3.4.6 |
| AC16 | **Perf budget (quantified concurrency):** `GET /dashboard/api/usage-series?granularity=hour` **p95 < 200 ms** measured under a **sustained 25 req/s (k6 `constant-arrival-rate`) for ≥ 60 s** (~25 concurrent viewers); `GET /dashboard` (static HTML) **p95 < 50 ms** under the same 25 req/s. True p95 by nearest-rank over captured latencies; `perf.scenario` records the 25 req/s rate + VU count + warm/cold cache. (`granularity=day` fan-out is measured + reported, advisory budget p95 < 600 ms, not gated — see Q1.) | perf harness; `src/services/dashboard_service.py`, `src/api/routes/dashboard.py` | k6 `constant-arrival-rate` @ 25 rps; record `perf.measured.p95_ms` + `perf.measured.throughput_rps` + `perf.scenario` for the hour path and the page | perf budget |
| AC17 | **DAST-1 readiness:** `/openapi.json` includes the BFF routes (`GET /dashboard/api/usage-series`, `/config`) with their params + response schema, matching the implemented routes; `GET /dashboard` is reachable and serves the expected security headers/CSP so the ZAP Layer-1 passive baseline (`.pipeline/dast.env`) scans a header-clean page. (`/docs` disabled in prod per ASVS 13.4.2/13.4.5.) | `src/main.py`, `src/api/schemas/dashboard.py`, `src/api/middleware.py` | schema-reachable + route/schema parity test; header-presence assertion on `GET /dashboard` | DAST-1 |
| AC18 | **DAST-2/3 auth context:** the existing seeded low-privilege DAST API key still authenticates the *authenticated* API routes; the scanner auth context `Authorization: Bearer mtr_live_<key_id>_<secret>` is documented (plan/docs). The dashboard page + BFF need **no client credential** to scan; the `dashboard-reader` key is **server-held from env/SSM, never hardcoded**. | `.pipeline/plan.md` §Auth, `docs/system_architecture.md`, `scripts/seed_api_key.py` (reused), `src/config/secrets.py` | presence check (`mtr_live` + `Bearer`) across plan/docs; Gitleaks/no-hardcoded-secret check on the reader key | DAST-2 / DAST-3 |
| AC19 | **`data_protection_waiver`: the feature stores no new user data.** It reads existing `usage_rollup` via `get_usage`; `customer_id` remains class=**personal**, protected at rest by the existing **RDS SSE (KMS)** (unchanged), redacted in logs, and served with `no-store`; the `dashboard-reader` key is class=**credential**, not stored by the app (runtime-fetched via the secrets facade, Argon2id-hashed in `api_keys`, CMK-encrypted in Secrets Manager). No new field is persisted → no new at-rest mechanism. | `src/logging/__init__.py`, `src/services/dashboard_service.py`, `src/config/secrets.py` | log-sink test: no raw `customer_id` in `dashboard.usage_series`; no-store assertion; secret-not-serialized assertion | data-protection (waiver: no new storage) |
| AC20 | **Environment badge reflects real config:** `CMP-2` is driven by `Settings.environment` via `GET /dashboard/api/config` — `prod` → prod variant (accent dot, "prod"), any other value → staging/grey variant labeled with the actual environment. Not a hardcoded design prop. | `src/config/settings.py`, `src/api/routes/dashboard.py`, `dashboard.js` | integration: config route returns the env; E2E: badge variant matches configured env | `CMP-2` / design-spec §6 |
| AC21 | **No new runtime deps / no migrations; two exactly-pinned dev deps:** the change adds **no** new runtime package (page served via FastAPI `FileResponse`; BFF reuses `get_usage`/secrets/auth) and **no** Alembic migration (no schema change). The two proposed **dev/test** deps are pinned **exactly** — `pytest-playwright==0.8.0` + `playwright==1.61.0` — and confined to the E2E tier (Q4). | `pyproject.toml` (unchanged runtime deps; dev-group pins if Q4 accepted), `alembic/versions/` (no new file) | dependency-pin test (no new runtime dep; exact dev pins, no wildcard); absence-of-migration check (no new `alembic/versions/000*`) | no-deps / no-migration |
| AC22 | **Smoke unchanged:** `GET /health` still returns **200** with no database dependency (this feature adds no lifespan/health change). | `src/api/routes/health.py` (unchanged) | global `smoke-check.sh` against `http://localhost:8000/health` | done: smoke |
| AC23 | **Window granularity resolved (Q1):** `hour` (default) and `day` are backed by live `get_usage` data (11 and bounded 264-read fan-out, within the endpoint's 90-day lookback); the `month` segment renders for fidelity but is **disabled with an affordance** (cannot be served correctly by the hourly rollup within the 90-day bound). The chosen alternative is recorded and matches the shipped behavior. | `src/services/dashboard_service.py`, `dashboard.js`, `.pipeline/plan.md` §Q1 | integration: hour + day series correct; E2E: month segment disabled + affordance; plan/doc records the decision | Q1 (design-vs-backend seam) |
| AC24 | **Safe error (BFF server envelope):** a forced internal error on the BFF (`/dashboard/api/usage-series` or `/config`) returns the **generic** envelope `{error:{code,message,requestId}}` with a correct status and **no** stack/SQL/secret/reader-key/`customer_id`/internal path; the read path has **no side effect** (fails closed). This is the server contract behind the client error card (AC7). | `src/api/errors.py` (reused), `src/services/dashboard_service.py` | safe-error test forces a BFF exception, asserts the envelope + no leak + correct status | ASVS 16.5.1 / 16.5.3 |
| AC25 | **Reader secret + least-privilege grant provisioned (IaC):** `infra/` provisions the `dashboard-reader` secret **CMK-encrypted** with the existing `aws_kms_key.data`, and the ECS task role gains **one resource-scoped** `secretsmanager:GetSecretValue` statement on **exactly that ARN** (no wildcard `Action`/`Resource`); the secret **value is set out-of-band**, never in `*.tfstate`/`*.tfvars`. | `infra/modules/data/main.tf`, `infra/modules/data/outputs.tf`, `infra/modules/compute/main.tf`, `infra/main.tf`, `scripts/seed_api_key.py` | `infra-validate.sh` (`fmt`/`validate`/`plan` → `.pipeline/infra-plan.txt`) + **Checkov** (secret KMS-encrypted, grant resource-scoped, no secret in state/tfvars) | IaC least-privilege (ASVS 13.2.2, threat E-D1) |

## Notes
- **No migration-reversibility criterion:** this feature adds no Alembic migration (no schema change),
  so the migration round-trip test mode does not trigger.
- **Input sources enumerated (for security's `input_surface` reconciliation):** exactly one untrusted
  source — the `GET /dashboard/api/usage-series` query string — accounted for by AC11 (validation) +
  AC12 (Tier-1 rate limit). `GET /dashboard`, the two static asset routes, and `GET /dashboard/api/config`
  take **no** untrusted input.
- **Design → plan → acceptance trace:** `SCREEN-1` → AC1; `CMP-1..CMP-9` → AC2–AC5, AC20; the two
  render states the export lacks → AC6/AC7; the two substantive spec items (real data vs mock, missing
  states) → AC8 + AC6/AC7.
- **Coverage:** `pytest --cov=src --cov-branch`, target **≥ 85% lines** per `CLAUDE.md` — surfaced,
  not gated. Hard testing gates remain acceptance-criteria completeness (`criteria_covered.covered >=
  .total`) and perf-pairing (AC16's budget must carry a measured value for both the p95 and throughput
  dimensions it names).
