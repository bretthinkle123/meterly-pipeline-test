---
criteria_total: 24
feature: "Meterly — metered-event ingestion + usage query (first feature)"
derived_from: "PROJECT.md 'What done means' + 'Non-functional / acceptance signals'"
notes: >
  Traces column maps each criterion to the verbatim PROJECT.md AC-* signal where one exists;
  the rest are functional / input-surface / data-protection / DAST-readiness criteria required
  by the pipeline conventions. Input sources exposed by this feature: (1) POST /v1/events body,
  (2) GET /v1/usage query. Each gets a validation criterion AND a rate-limit criterion.
---

# Acceptance criteria — Meterly first feature

| ID | Criterion | File / layer | How verified | Traces |
|---|---|---|---|---|
| AC1 | `POST /v1/events` with a valid API key and body returns **201** and persists exactly one `events` row; response echoes only `{event_id, customer_id, metric, quantity, window_start, idempotent_replay:false}`. | `src/api/routes/events.py`, `src/services/events_service.py` | integration test (testcontainers PG): 201 + one row + minimal fields | functional |
| AC2 | Duplicate `idempotency_key` (same `api_key_id`) is a **no-op**: returns **200**, creates no new `events` row, and does **not** increment `usage_rollup`. | `src/repositories/events_repo.py` (`ON CONFLICT DO NOTHING`) | replay test asserts row count unchanged + `event_count` unchanged | AC-CONCURRENCY, functional |
| AC3 | The idempotent replay body equals the **original** event's representation with `idempotent_replay:true` (replay semantics pinned even after intervening events). | `src/services/events_service.py` | replay test pins the returned body to the first event | functional |
| AC4 | `GET /v1/usage?customer_id=&metric=&window=` returns the correct `{total_quantity, event_count}` for the hour bucket (window floored to the hour, UTC). | `src/services/usage_service.py`, `src/repositories/usage_repo.py` | integration test: seed events, assert aggregate | functional |
| AC5 | `GET /v1/usage` for a window with no data returns **200** with `total_quantity:0, event_count:0` (never 404). | `src/services/usage_service.py` | test asserts 200 + zeros for an empty bucket | functional |
| AC6 | **AC-PERF:** `POST /v1/events` p95 **< 50 ms** measured under **500 req/s sustained** (not serial) over a distributed key space, with throughput **≥ 475 req/s**; `GET /v1/usage` p95 **< 100 ms**. | perf / rollup read path; `src/observability` | k6 `constant-arrival-rate`; true p95 by nearest-rank over captured latencies; record `perf.measured.{p95_ms,throughput_rps}` and `perf.scenario` | **AC-PERF** |
| AC7 | **AC-CONCURRENCY:** 50 concurrent POSTs with the **same** `idempotency_key` create **exactly one** `events` row; the 49 losers each receive the idempotent replay (200); `usage_rollup.event_count` increments **once**. | `src/repositories/events_repo.py` | concurrency harness (`asyncio.gather`/threads) fires 50 identical requests; asserts one row + loser outcome + single increment | **AC-CONCURRENCY** |
| AC8 | **AC-DATA-PROTECTION:** `api_keys.secret_hash` is stored as an **Argon2id** hash (class=credential); the raw stored column is **never** the plaintext key; `verify(secret)` still succeeds. | `src/crypto/__init__.py`, `src/repositories/api_keys_repo.py` | persisted-form test reads the raw column, asserts Argon2id hash (not plaintext) + verify round-trips | **AC-DATA-PROTECTION** |
| AC9 | `data_protection` — `customer_id` (events + usage_rollup) class=**personal**, mechanism=**SSE (RDS KMS)**; persisted DB is encrypted at rest and `customer_id` is never logged/Sentry-sent in raw form. | `infra/modules/data`, `src/logging`, `src/observability/sentry.py` | Checkov `storage_encrypted=true`; log-sink test asserts no raw `customer_id` in log output | data-protection |
| AC10 | **AC-MIGRATION:** migration **0002** (`usage_rollup` create + backfill) round-trips `up→down→up` on a **prod-shaped seeded** dataset — the seeded `events` rows are preserved and `usage_rollup` is deterministically re-derived identically. | `alembic/versions/0002_create_usage_rollup_backfill.py` | migration round-trip test seeds events, up/down/up, asserts events preserved + rollup identical | **AC-MIGRATION** |
| AC11 | Migration **0001** (create `api_keys` + `events`) is reversible as **schema + constraints**: `up→down→up` recreates the schema and re-enforces every CHECK/FK/UNIQUE/NOT NULL identically (create-migration: literal row survival across `down` is not asserted). | `alembic/versions/0001_create_api_keys_and_events.py` | round-trip test re-seeds after the second `up` and re-verifies each constraint | AC-MIGRATION |
| AC12 | **AC-SLO:** availability **99.9%** and ingest **p95 < 50 ms** are defined as SLOs with multi-window burn-rate alarms; the named alarms (`meterly-prod-slo-availability-fastburn`, `meterly-prod-slo-ingest-p95-fastburn`, `meterly-prod-5xx-rate`, `meterly-prod-alb-p95-latency`, `meterly-prod-unhealthy-hosts`) exist and are referenced by `deploy.yml`'s `<PROD_ALARM_NAMES>`. | `infra/modules/observability`, `envs/prod` | Terraform plan/apply-check asserts the alarm resources + the SLO doc; names wired to the canary | **AC-SLO** |
| AC13 | **POST /v1/events input validation:** contracts enforced — `customer_id` `^[A-Za-z0-9_.:-]{1,128}$`, `metric` `^[A-Za-z0-9_.:-]{1,64}$`, `quantity` decimal `>0, ≤1e12`, `idempotency_key` `^[A-Za-z0-9_-]{1,200}$`, `extra='forbid'`; malformed/wrong-type → **422**, body >8 KiB → **413**; injection payloads (`' OR 1=1--`, `../`, CRLF) rejected at the boundary and parameterized at the SQL sink. | `src/api/schemas/events.py`, `src/api/middleware.py`, `src/repositories/events_repo.py` | Hypothesis + explicit malformed/injection tests | input-surface validation |
| AC14 | **POST /v1/events rate limit:** Tier-2 per-owner throttle keyed on **`api_key_id`**, limit = key's `rate_limit_per_sec` (default 100/s) token bucket per 1 s window; over-limit → **429 + `Retry-After`**. Discriminating test: **two principals sharing one client IP** get independent buckets; one principal across two IPs shares a bucket. | `src/auth/rate_limit.py` (Redis) | two-principals-one-IP integration test | input-surface rate-limit |
| AC15 | **GET /v1/usage input validation:** `customer_id`/`metric` allowlist contracts; `window` must be a **timezone-aware** ISO-8601 timestamp within `[now-90d, now+1h]` (naive datetimes and out-of-range rejected → 422). | `src/api/schemas/usage.py` | validation tests incl. naive-datetime + out-of-range | input-surface validation |
| AC16 | **GET /v1/usage rate limit:** Tier-2 per-owner throttle keyed on **`api_key_id`** (same policy as AC14); over-limit → **429 + `Retry-After`**. | `src/auth/rate_limit.py` | two-principals-one-IP test on the read path | input-surface rate-limit |
| AC17 | **Tenant isolation (IDOR/BOLA):** an authenticated key **B** cannot read or write key **A**'s data — every repo query is scoped by the authenticated `api_key_id` and PostgreSQL RLS is the backstop (app role without `BYPASSRLS`). B querying A's `customer_id` gets B's own zero, never A's totals. | `src/repositories/*`, `src/db/session.py` | cross-owner denial test (two keys) for read and write | ASVS 8.2.2 |
| AC18 | **Auth required:** a request with a missing, malformed, invalid, or revoked API key returns **401/403**, never 200 or the resource. | `src/auth/__init__.py`, `src/auth/api_key.py` | unauthenticated-denial test across all four cases | auth boundary |
| AC19 | **Safe error:** a forced internal error returns the **generic** envelope `{error:{code,message,requestId}}` with **no** stack/SQL/secret/key/internal path, a correct status, and **fails closed** (event + rollup are one transaction — no partial write). | `src/api/errors.py`, `src/services/events_service.py` | safe-error test forces an exception mid-write; asserts envelope + no partial row | ASVS 16.5.1/16.5.3 |
| AC20 | **ASVS L1/L2** for every triggered chapter (V1,V2,V4,V6,V8,V11,V12,V13,V14,V15,V16) is met or waived, and the in-scope **L3** items (11.2.4 constant-time compare, 15.4 TOCTOU-atomic) are met. | whole change set; plan `## ASVS Compliance` | security agent step 6g reconciliation | ASVS |
| AC21 | **Smoke:** `GET /health` returns **200** with no database dependency (liveness); `/health/ready` performs the DB/migration check for the ALB. | `src/api/routes/health.py` | global `smoke-check.sh` against `http://localhost:8000/health` | done: smoke |
| AC22 | **DAST-1:** the app serves `/openapi.json` matching the implemented routes, reachable in non-prod (Swagger `/docs` disabled in prod). | `src/main.py` (FastAPI) | schema-reachable test + route/schema parity check | DAST-1 |
| AC23 | **DAST-2:** a seeded low-privilege DAST test API key exists in staging/local via `scripts/seed_api_key.py`; its secret comes from env/SSM and is **never** hardcoded. | `scripts/seed_api_key.py` | seed-script run + Gitleaks/no-hardcoded-secret check | DAST-2 |
| AC24 | **DAST-3:** the scanner auth context is documented — credential presented as `Authorization: Bearer mtr_live_<key_id>_<secret>` (header name + token shape recorded) so ZAP/Schemathesis can authenticate. | plan §Auth, `docs/` | presence of the documented auth-context in the plan/docs | DAST-3 |

## Coverage note (surfaced, not gated)
`pytest --cov=src --cov-branch`, target **≥ 85% lines** per `CLAUDE.md`. The hard testing-side gates
are acceptance-criteria completeness (`criteria_covered.covered >= .total`) and perf-pairing (AC6's
declared budget must have a measured value); line/branch coverage is surfaced and reviewed at the
human diff checkpoint.
