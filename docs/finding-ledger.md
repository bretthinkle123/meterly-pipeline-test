# Finding ledger (U-10)

Every escaped finding gets exactly one decision. A row's `action` MUST be one of:
`new efficacy question` / `new planted eval defect` / `new deterministic check` /
`accepted:<reason>`. An escape with no decision is itself a defect.

Bootstrapped 2026-07-10 by the M4″ audit (feature 4, quota-admin) from the 14
verifier-CONFIRMED `/code-review` findings the human deferred at diff approval,
plus the 2 PLAUSIBLE findings and the security stage's 2 non-blocking warnings.
Features 1–3 predate this file; their deferred findings live in orchestrator
memory and should be back-filled here on next touch.

| # | Finding | Class | Escaped because | Action |
|---|---------|-------|-----------------|--------|
| F4-01 | `GET /v1/quotas` silently ignores unknown query params (`src/api/routes/quotas.py:68`) — `?customer_id=X` returns the FULL tenant list, against the API-wide `extra='forbid'` convention | validation-contract gap (param-less endpoint) | Planning's validation-contract table covers declared params only; a param-less endpoint has no row, so no stage owned "reject the undeclared". Passed planning, implementation, security (6c checks declared inputs), testing, and both plan audits; caught only by /code-review | new deterministic check — planning must emit an explicit no-params contract (an AC or an N/A-with-rationale) for every endpoint that declares no query/body params, and testing must assert unknown-param behavior (422 under `extra='forbid'`) per route; engine delta filed in the M4″ audit |
| F4-02 | AC19 fail-closed test is vacuous — monkeypatch raises before any SQL executes; the rollback path is never exercised (`tests/integration/test_quotas_list_delete.py`) | vacuous test | The test passes for a reason unrelated to the property claimed; line/branch coverage can't see it and mutmut is inert on win32 | new planted eval defect (U-23) — plant a pre-SQL-raise vacuous fail-closed test in the engine eval suite; the testing agent must flag it in its adversarial review |
| F4-03 | AC16 redaction test is vacuous — the seed PUT's log line already contains `***redacted***`; the raw value is never asserted absent | vacuous test | Same class as F4-02: the assert cannot fail even if redaction breaks | new deterministic check — falsifiability probe: for each security-property test (redaction, fail-closed, authz), the testing agent must show the assert CAN fail (violate the property once, observe red, restore) and record the probe in `test-quality.json`; engine delta filed in the M4″ audit |
| F4-04 | Migration-count time bomb — a test asserts exactly 3 alembic version files; the next migration breaks it | brittle exact-count assert | Reads as an intentional invariant; no check distinguishes "pinned count" from "count that grows by design" | new deterministic check — ast-grep rule flagging exact-count asserts over migration/revision listings (`len(...) == N` on alembic version dirs) |
| F4-05 | Unbounded `list_quotas` SELECT — no LIMIT anywhere, ~4× materialization; DoS lever at scale (unpaginated was operator-pinned; the absence of ANY cap was not) | missing non-functional bound | Elicitation asked "paginate?" and accepted "no", but no question forced an expected-max or behavior-at-excess; planning inherited the pin as "no bound needed" | new efficacy question (U-02) — whenever an endpoint returns an unpaginated collection, elicitation/planning must record an explicit expected-max + at-excess behavior or an accepted-risk note |
| F4-06 | CORS DELETE method broadened app-wide; no test pins `allow_methods` (latent — `origins` defaults empty) | untested config regression | CORS is inert with empty origins, so no behavioral test could catch the broadening | accepted: latent-no-exposure — CORS is inert today (empty origins); MUST pin the allow_methods/origins matrix with a test when origins are first configured |
| F4-07 | `quota_service` docstring falsely claims a read-only transaction | stale doc/comment | Comment accuracy isn't scanned; human deferred | accepted: doc-only — fix on next touch of `quota_service.py` |
| F4-08 | `QuotaListItem` is a `QuotaResponse` clone; field set hand-maintained in 4 places | duplication | Quality-class finding, deliberately deferred at diff approval | accepted: refactor-backlog — consolidate on next schema touch |
| F4-09 | Six inline `create_async_engine` blocks in `test_quotas_list_delete.py`, no try/finally | test hygiene | Deferred at diff approval | accepted: test-hygiene — extract a fixture on next touch of the file |
| F4-10 | 4th copy of the two-principal throttle test | duplication | Deferred at diff approval | accepted: duplication — extract a shared helper when a 5th consumer appears or on next touch |
| F4-11 | Schema tests duplicated with a second regex oracle | duplication | Deferred at diff approval | accepted: duplication — single oracle on next touch |
| F4-12 | 204 status encoded twice in the DELETE route | trivial duplication | Deferred at diff approval | accepted: trivial — fold into next touch of `routes/quotas.py` |
| F4-13 | Service raises `HTTPException(404)` while the sibling path returns an outcome dataclass | layering inconsistency | Deferred at diff approval | accepted: consistency-refactor-backlog — unify on the outcome-dataclass pattern next service touch |
| F4-14 | 3rd/4th copies of the safe-500 assertion block | duplication | Deferred at diff approval | accepted: duplication — shared assertion helper on next touch |
| F4-P1 | (PLAUSIBLE) `customer_id` travels in the DELETE query string vs the redaction regime — no access-log sink today | latent PII-in-URL | Pre-existing pattern; no sink exists to leak into | accepted: watch — becomes a real finding the day an access-log/proxy-log sink is added; revisit then |
| F4-P2 | (PLAUSIBLE) `_require_admin_and_throttled` worth promoting to `src/auth` `require_scope()` | refactor suggestion | Quality suggestion, not a defect | accepted: refactor-backlog — promote when a third route family needs the gate |
| F4-S1 | pytest 8.3.4 CVE (PYSEC-2026-1845 / GHSA-6w46-j5rx-g56g, CVSS 6.8, dev-only) — waived in `osv-scanner.toml`, `ignoreUntil` 2026-10-09 | sub-floor dependency CVE | Below the ≥7.0 gate floor and dev-only; was masked earlier by the OSV exit-127 step bug (scanner-liveness, see M4″ audit A9) | accepted: waived-dev-only-below-floor — operator MUST re-review at the 2026-10-09 waiver expiry (bump to pytest 9.x or re-waive consciously) |
| F4-S2 | Tier-1 throttle IP-keying behind the ALB (pre-existing) | topology efficacy (U-02 class) | Pre-existing on main; out of this feature's diff scope | accepted: pre-existing-backlog — already on the engine/app backlog; fix with the ProxyHeaders/forwarded-allow-ips work |
| F5-01 | `SUM(event_count)` over the `BigInteger` column returns SQL numeric → asyncpg yields a `Decimal` stored in `DailyMetricCount.event_count` annotated `int` (`src/repositories/usage_repo.py:246`) | runtime-type mislabel (masked) | Response JSON is correct today (Pydantic `DailyUsageResponse` re-coerces to int), so no test/coverage/security signal fires; caught only by /code-review | new deterministic check — ast-grep rule flagging SQL aggregate (`SUM`/`COUNT`) results bound to `int`-annotated fields without an explicit `int()` cast; add the `int()` cast in `usage_repo` on next touch |
| F5-02 | 90-day lookback bound triplicated across `usage.py` / `usage_export.py` / `usage_daily.py`, with drifted units | duplication / drift risk | DRY across pre-existing sibling endpoints; each copy is independently valid and no cross-module check exists | accepted: refactor-backlog — consolidate the lookback-window policy into one shared helper before a 4th consumer; units already drifted, so reconcile on next touch |
| F5-03 | `_require_authenticated_and_throttled` copied verbatim into 3 route modules (`usage`/`usage_export`/`usage_daily`) | duplication | Same class as F4-P2 (the admin variant); deferred at diff approval | accepted: duplication — promote to a shared `src/auth` dependency when a 4th route family needs it (tracks with F4-P2) |
| F5-P1 | (PLAUSIBLE) repo dataclass `DailyMetricCount` name/field collision with the Pydantic `DailyMetricCount` schema one import away | naming collision | Quality-class; both kept field-compatible by hand, against the repo's `UsageRollupRecord` naming convention | accepted: naming-refactor-backlog — rename the repo record (e.g. `DailyMetricCountRecord`) on next touch of `usage_repo.py` |
| F5-S1 | The `events` table (migration `0001`) shares the non-FORCE-RLS owner-bypass that `0004` fixed for `usage_rollup`; its `events_tenant_isolation` RLS backstop is inert under the owner role `meterly_app` | pre-existing latent security gap | Flagged "for awareness" by the security stage; deliberately left out of usage-daily's diff scope (the app-level `api_key_id`-first predicate remains the primary control) | new deterministic check — follow-up migration FORCE-enabling RLS on `events` + an owner-role isolation test (mirror `0004` / `test_usage_rollup_rls_backstop.py`). **REMEDIATED 2026-07-12** by migration `0005_force_rls_events.py` + `tests/integration/test_events_rls_backstop.py` (feature events-force-rls, PR #9, merge `a4e1903`); the `events` backstop is now owner-bound `FORCE` RLS with a fail-before-0005/pass-after owner-role efficacy test |
| F6-01 | `tests/integration/test_events_rls_backstop.py` is a near-verbatim copy of `test_usage_rollup_rls_backstop.py` (owner-role fixture + two RLS-efficacy tests), differing only in table/column/label names — now the 3rd copy alongside quotas/usage_rollup | duplication | Surfaced by the pre-checkpoint `/code-review` (medium, unverified) and deferred at diff approval; the one-file-per-table backstop layout is the established repo convention the plan deliberately mirrors | accepted: duplication — same class as F5-03/F4-10; promote the owner-role RLS-backstop fixture+assertions to a shared parametrized helper when a 4th table needs the proof, or on next touch |
| F6-02 | `events_owner_role` fixture globally reassigns the `events` table owner on the shared session-scoped testcontainer for the test's duration (`test_events_rls_backstop.py:100`); correctness depends on serial test execution | latent test-isolation hazard | Pre-existing pattern (mirrors `test_usage_rollup_rls_backstop.py`); moot today because no `pytest-xdist`/parallelism is configured (verified) | accepted: watch — same watch-class as F4-P1/F4-06; becomes a real flakiness source the day parallel test execution (`-n`/xdist) is enabled — must gate ownership-mutating RLS-backstop tests to a serial group before then |
